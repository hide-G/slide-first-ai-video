/**
 * Marp rendering Lambda handler.
 * Converts Marp Markdown to PNG slide images.
 * Implementation pending.
 */
export const handler = async (): Promise<void> => {
  throw new Error("Not implemented");
};
