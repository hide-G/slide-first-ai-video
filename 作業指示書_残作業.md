# 作業指示書: 残作業の実装とデプロイ

この文書は、**このリポジトリの残作業を実行するための作業指示**です。何を作るかは別文書に確定済みです。この文書は「どの順で、どの環境で、何をどう確認して進めるか」を定めます。

## 0. 最初に読む順番

1. **この文書**（作業の進め方、環境情報、完了条件）
2. `設計指示書_動画生成パイプライン.md` — 動画生成パイプラインの実装内容。**実機検証済みの値が入っているので最優先の正本**
3. `実装指示プロンプト.md` — アプリ全体の仕様。冒頭の「重要な改訂」節を必ず読むこと
4. `docs/contract.md` — 現在のデータ契約（この作業で更新する）
5. `mockup/index.html` をブラウザで開く — UIの正本

推測で設計を変えないでください。判断できない点は「不明点」として報告してください。

---

## 1. 現在の状態

### 1.1 リポジトリ

- ブランチ `main`、コミット `5847d1c`
- 直近のマージ: PR #8（動画生成パイプラインの設計指示書を追加）

### 1.2 デプロイ済みのAWS環境

CloudFormationスタック `ltvideo-dev` が稼働中です（100リソース、`UPDATE_COMPLETE`）。ただし**動画生成は動きません。**

| 動く                                                                                                                         | 動かない                                                     |
| ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| ログイン（Cognito）、API、DynamoDB、S3、CloudFront、フロントエンド配信、骨子・ナレーション生成（Bedrock）、音声合成（Polly） | 工程1 ページ画像化、工程3 字幕、工程4 ページ動画、工程5 連結 |

動かない理由は3つです。詳細と対処は設計指示書に記載しています。

1. `marp-render`、`caption-worker`、`clip-worker`、`concat-worker` のZip資産が `tsc` の出力のみで、`node_modules` を含まない。`Runtime.ImportModuleError` で起動しない
2. `ffmpeg`、`ffprobe`、`pdftoppm`、`libreoffice` を呼ぶが、`nodejs22.x` ランタイムに存在しない
3. `deck.ts` が `action: "generateDeck"` で `marp-render` を呼ぶが、marp-render側に該当処理がなく、Marpを実行するコードがどこにもない

---

## 2. 環境情報

### 2.1 AWS

| 項目                    | 値                                                  |
| ----------------------- | --------------------------------------------------- |
| アカウント              | `285336573977`                                      |
| プロファイル            | `nagata`（`default` も同一アカウント）              |
| リージョン              | `ap-northeast-1`                                    |
| スタック名              | `ltvideo-dev`                                       |
| S3バケット              | `ltvideo-projects-dev-285336573977-ap-northeast-1`  |
| CDKブートストラップ     | 済み（`CDKToolkit` スタックあり）                   |
| MediaConvert            | 利用可能。`Default` キューが `ON_DEMAND` / `ACTIVE` |
| MediaConvert用IAMロール | **未作成。CDKで作成する**                           |

`jawsdays` というプロファイルは存在しません。使わないでください。

### 2.2 開発機の制約

| 項目        | 内容                                                |
| ----------- | --------------------------------------------------- |
| OS          | MacBook上のBoot Campで動くWindows 10                |
| Docker      | **導入できません。** コンテナイメージ方式は採用不可 |
| Node.js     | 24.15.0                                             |
| pnpm        | 10.28.1                                             |
| AWS CLI     | 2.31.1                                              |
| GitHub CLI  | 2.83.2                                              |
| AWS SAM CLI | 未インストール                                      |
| CDK CLI     | ローカル未導入。`npx aws-cdk@2` で実行する          |

### 2.3 PowerShellの注意点

日本語版WindowsのPowerShell 5.1は、外部コマンドのUTF-8出力をCP932として解釈します。`git`、`gh`、`aws` の日本語が文字化けして見えますが、**表示だけの問題**です。

- 出力を読む前に `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` を設定する
- 文字化けを見て「失敗した」と判断しない。終了コードと成果物で判断する
- `2>&1` は標準エラーをエラーレコード扱いにするため、`git push` が成功しても赤い表示になる
- `Set-Content -Encoding UTF8` はBOM付きになる。Node.js の `JSON.parse` へ渡す前にBOMを除去する

詳細は `PDFからMP4を作成する方法.md` の第8.3節にあります。

### 2.4 ビルドとCDKの実行方法

`infra/cdk.json` の `app` は `node dist/bin/app.js` です。**CDKを実行する前に必ずビルドしてください。**

```powershell
# リポジトリルートで
pnpm install
pnpm build
pnpm test

# infra ディレクトリで
$env:AWS_PROFILE = "nagata"
$env:AWS_REGION = "ap-northeast-1"
npx --yes aws-cdk@2 synth
npx --yes aws-cdk@2 diff
```

`Code.fromAsset("../lambdas/<name>/dist")` が相対パスなので、**`infra` ディレクトリから実行してください。**

---

## 3. 残作業の全体像

4つのフェーズに分かれます。フェーズをまたぐ前に報告してください。

| フェーズ | 内容                            | 承認                               |
| -------- | ------------------------------- | ---------------------------------- |
| 1        | 実装（設計指示書のタスク1〜15） | 不要。1タスクずつ進めて都度報告    |
| 2        | AWSへのデプロイ                 | **必須。差分を提示して承認を得る** |
| 3        | 実機での動作検証                | 不要。結果を報告                   |
| 4        | 記録の更新                      | 不要                               |

---

## 4. フェーズ1: 実装

`設計指示書_動画生成パイプライン.md` の第7節にあるタスク1〜15を、**番号順に1つずつ**実施します。

各タスクで必ず守ること。

- 1タスクで複数の関心事を同時に変更しない
- タスクごとに `pnpm build` と `pnpm test` を通す
- タスク完了時に第8節の形式で報告する
- 検証していない項目は「未検証」と明記する

フェーズ1の完了条件です。

1. `pnpm install && pnpm build && pnpm test` が成功する
2. `npx aws-cdk@2 synth` が成功する
3. すべてのLambda資産に、解決できない `require` が残っていない
4. `ffmpeg`、`ffprobe`、`pdftoppm`、`libreoffice` を呼ぶコードが存在しない
5. `Dockerfile` が存在しない
6. `cdk.out` の資産を実際に開いて中身を確認した

---

## 5. フェーズ2: デプロイ

### 5.1 デプロイ前に必ず行うこと

```powershell
cd infra
$env:AWS_PROFILE = "nagata"
$env:AWS_REGION = "ap-northeast-1"
npx --yes aws-cdk@2 diff
```

差分は行数が多くなります。**次の観点で整理してから提示してください。**

- 削除されるリソースの一覧（種別と名前）
- 追加されるリソースの一覧
- **データを持つリソース（S3バケット、DynamoDBテーブル、Cognitoユーザープール）が置き換えられないこと**
- `replace` という語を含む警告があるかどうか

差分の行数が多くて読み切れない場合は、デプロイ済みテンプレートを取得して論理IDで比較すると正確です。

```powershell
aws cloudformation get-template --stack-name ltvideo-dev --template-stage Original `
  --region ap-northeast-1 --query "TemplateBody" --output json > deployed.json
# deployed.json と cdk.out/ltvideo-dev.template.json の Resources を論理IDで比較する
```

### 5.2 参考: 前回確認した差分（設計変更前の値）

同種の確認を行った際の実測値です。**今回の実装後は値が変わるので、必ず取り直してください。**

- デプロイ済み114リソース、synth結果116リソース、削除59・追加61・維持55
- 削除されたのは旧構成のみ: Lambda 4個（`composition-builder`、`renderer`、`teaser-generator`、`teaser-composition-builder`）、Step Functions 2個（`content-generation`、`teaser-pipeline`）、SQS 1個（`approval-queue`）、API Gateway 26個
- データを持つリソースはすべて維持。`DeletionPolicy` は S3 ProjectBucket、DynamoDBテーブル、Cognitoユーザープールがいずれも `Retain`

### 5.3 承認を得てからデプロイする

**差分を提示し、ユーザーの承認を得てから実行してください。** 承認なしでデプロイしないこと。

```powershell
npx --yes aws-cdk@2 deploy --require-approval broadening
```

デプロイ後に確認すること。

- スタックが `UPDATE_COMPLETE` になったこと
- 新しいLambda（`caption-worker`、`mediaconvert-worker`）が作成されたこと
- MediaConvert用IAMロールが作成され、ジョブ投入側に `iam:PassRole` が付与されていること
- `PassRole` の対象がそのロールのARNだけに限定されていること
- S3バケットが公開されていないこと

---

## 6. フェーズ3: 実機での動作検証

デプロイ後、**実際に動画を1本作って数値で確認します。** 「デプロイ成功」は動作確認ではありません。

### 6.1 テスト用ユーザーの用意

Cognitoにユーザーがいない場合は作成します。ユーザープールIDはスタックの出力またはCloudFormationのリソースから取得してください。

```powershell
aws cognito-idp admin-create-user --user-pool-id <POOL_ID> `
  --username <メールアドレス> --message-action SUPPRESS `
  --profile nagata --region ap-northeast-1

aws cognito-idp admin-set-user-password --user-pool-id <POOL_ID> `
  --username <メールアドレス> --password <パスワード> --permanent `
  --profile nagata --region ap-northeast-1
```

**認証情報をログや報告に書かないでください。**

### 6.2 検証する2つの経路

**経路A: ②スライド生成から動画まで**

1. プロジェクト作成
2. 骨子を生成し、**1ページの本文を編集**してから確定する
3. スライドを生成し、`deck.md`、`deck.pdf`、`deck.pptx`、ページPNGがS3に作られることを確認
4. 手順2で編集した文章が `deck.pdf` に反映されていることを確認
5. ナレーションを生成し、確定する
6. 動画を生成する

**経路B: ③PDFアップロードから動画まで**

1. 手元のPDF（5ページ程度）をアップロードする
2. ②を経由せずに動画が完成することを確認する
3. `.pptx` をアップロードすると HTTP 400 と `PPTX_NOT_SUPPORTED` が返ることを確認する

### 6.3 数値で判定する項目

| 項目              | 判定                                                                             | 取得方法                                                    |
| ----------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| ページ数とPNG枚数 | 一致                                                                             | S3の `pages/` を数える                                      |
| 音声の長さ        | PCMのバイト数から算出した値と `manifest.json` の `audioDurationSec` が一致       | manifestとS3のオブジェクトサイズ                            |
| フレーム丸め      | `frameAlignedDurationMs` が `ceil(音声ms / 33.3333) × 33.3333` の丸め値と一致    | manifest                                                    |
| 動画の長さ        | `GetJob` の `DurationInMs` と `frameAlignedDurationMs` の合計の差が **50ms以内** | MediaConvertの `GetJob`                                     |
| 字幕              | 件数がページ数と一致し、時刻が単調増加、累積が丸め後合計と一致                   | `captions/captions.srt`                                     |
| 日本語表示        | ページPNGで日本語が正しく描画される（□にならない）                               | PNGをダウンロードして目視、および設計指示書 第2.2節の幅比較 |
| 出力サイズ        | `9:16` を選ぶと 1080×1920 になる                                                 | `GetJob` の `VideoDetails`                                  |

動画の長さは `GetJob` から取得できます。ffprobeは使いません。

```text
Job.OutputGroupDetails[0].OutputDetails[0].DurationInMs
Job.OutputGroupDetails[0].OutputDetails[0].VideoDetails
```

### 6.4 失敗したときの調べ方

- Step Functionsの実行履歴で、どの工程で止まったかを特定する
- 該当LambdaのCloudWatch Logsを見る。`Runtime.ImportModuleError` が出ていたらパッケージングの問題（設計指示書 第3節）
- MediaConvertのジョブは `GetJob` の `ErrorMessage` と `ErrorCode` を見る
- 工程単位で再実行できる設計なので、失敗した工程だけを `startFromStage` で再開して切り分ける

### 6.5 後片付け

検証で作った一時的なリソースは削除してください。削除したことを報告に含めてください。

- 検証用に作ったIAMロール、Lambda、S3オブジェクト
- 不要になったCloudWatchロググループ

---

## 7. フェーズ4: 記録の更新

1. `docs/contract.md` を実装に合わせて更新する（`frameAlignedDurationMs`、`audioKey` の `.wav`、許容差、`cost` に `mediaconvert`）
2. `docs/migration.md` に、削除したもの（`clip-worker`、`concat-worker`、`Dockerfile`、FFmpeg関連コード）と理由を追記する
3. `docs/cost.md` に、MediaConvertの費用の扱いと、コスト配分タグを請求コンソールで有効化する手順を追記する
4. 実機検証で分かった新しい事実（実測値、つまずいた点と対処）を `設計指示書_動画生成パイプライン.md` に追記する

---

## 8. 報告の形式

各タスク・各フェーズの終わりに、次の形式で短く報告してください。

```text
タスク: <番号と名前>
変更したファイル: <一覧>
実行した検証: <コマンドと結果の要点>
数値の実測値: <該当する場合。例: 丸め後合計 139,100ms / DurationInMs 139,133ms / 差 33ms>
未検証の項目: <あれば明記。無ければ「なし」>
不明点: <あれば1〜3件>
```

「動いた」ではなく数値で報告してください。検証していない項目は必ず「未検証」と書いてください。

---

## 9. Gitの進め方

このリポジトリの運用ルールです。

- 作業ブランチは**必ず `main` から**作成する。作業中のブランチから別のブランチを生やさない
- `main` へ直接プッシュしない
- 互いに関係のない変更は別ブランチにする
- プルリクエストを作る前に、変更内容とPRの文章をユーザーへ提示して承認を得る
- **プルリクエストのマージは人間が行う。** 自分でマージしないこと
- 生成した動画・音声・PDF・PNGをコミットしない（`artifacts/` と `dist.zip` は `.gitignore` 済み）

推奨するブランチの分け方です。

| ブランチ                      | 内容                                                 |
| ----------------------------- | ---------------------------------------------------- |
| `feat/bundle-workers`         | 設計指示書のタスク1〜3（バンドルと共通関数）         |
| `feat/wav-audio-and-contract` | タスク4〜6（WAV化、契約更新、字幕）                  |
| `feat/mediaconvert-worker`    | タスク7〜9（MediaConvert化とCDK）                    |
| `feat/marp-deck-generation`   | タスク10〜12（Marpによるスライド生成とラスタライズ） |
| `feat/pptx-policy-and-cost`   | タスク13〜14                                         |

1つのPRが大きくなりすぎる場合は、さらに分けてください。

---

## 10. 禁止事項

- 承認なしで `cdk deploy` を実行すること
- 承認なしでプルリクエストを作成・マージすること
- Dockerやコンテナイメージを使う方式を提案・実装すること
- `ffmpeg`、`ffprobe`、`pdftoppm`、`libreoffice` を使うこと
- `tsc` の出力だけを `Code.fromAsset` に渡すこと
- 設計指示書 第2節の検証済みの値・設定・バージョンを推測で変更すること
- `audioDurationSec` に推定値を入れること
- 失敗を隠すフォールバック（中身の違うファイルを返す、例外を握りつぶす、空ファイルを返す）
- モックアップに無い機能・画面・設定項目を追加すること
- 認証情報・トークン・個人情報をログや報告へ出力すること
- 検証していないのに「完了」と報告すること

---

## 11. 全体の完了条件

1. `pnpm install && pnpm build && pnpm test` が成功する
2. `npx aws-cdk@2 synth` が成功し、`deploy` が `UPDATE_COMPLETE` で完了する
3. 経路A（②スライド生成から動画まで）が完走する
4. 経路B（③PDFアップロードから動画まで）が完走する
5. `.pptx` のアップロードで HTTP 400 と `PPTX_NOT_SUPPORTED` が返る
6. 動画の長さと `frameAlignedDurationMs` の合計の差が50ms以内
7. ページPNGと焼き込み字幕で日本語が正しく表示される
8. `9:16` を選ぶと 1080×1920 の動画が出力される
9. 工程ごとの推定コストが表示され、MediaConvertの行が含まれる
10. モックアップにあるボタン・入力項目が、すべて対応する処理へつながっている
11. 認証なしでAPIへ到達できない。S3バケットが公開されていない
12. 検証で作った一時リソースがすべて削除されている
13. `docs/` の3ファイルが実装に合わせて更新されている

---

## 2026-08-15 Video Studio 改善作業ログ

### 目的

PDFアップロードからの動画生成について、次の利用上の課題を解消する。

1. PDFから抽出できている本文を、ページごとのナレーション原稿欄へ初期入力する
2. 「このページにAIナレーション案を挿入」の実行状態・成功・失敗を画面内で明示する
3. ナレーション付き動画で字幕を正しく生成・焼き込みできるようにする
4. バックグラウンド実行中の工程・ページ進捗を表示し、ブラウザを閉じてもトップ一覧から再開・再ダウンロードできるようにする
5. ダウンロードするMP4を、元PDF名とJST開始日時で識別できる名前にする

### 調査で確定した原因

- PDF本文はブラウザで抽出・プレビューできていたが、`VideoStudioPage`が`narrationPages[].script`へ保存していなかった。
- AIナレーション案の失敗は画面最上部にだけ表示され、ページ編集中には無反応に見えた。
- AI案生成で使うBedrockモデル`apac.anthropic.claude-sonnet-4-20250514-v1:0`は、プロバイダー側でLegacy扱いとなり、実行時に拒否されていた。
- 無音動画を一度選ぶと字幕設定を`none`にしたまま、音声ありへ戻しても復元しない画面状態があった。このため、字幕WorkerとMediaConvertの実装が正しくても、manifestに`captions: "none"`が保存され、SRT・焼き込み字幕が生成されなかった。
- レンダリング状態はStep Functionsの工程名だけを返し、ページ番号・総ページ数・処理メッセージを返していなかった。
- プロジェクト一覧は最新レンダリング情報を持たず、完成後の動画を開く・再ダウンロードする導線がなかった。
- 成果物URLはS3キー由来の名前で返しており、`ResponseContentDisposition`を指定していなかったため、`page-001-video.mp4`のような名前になっていた。

### 採用した設計

- PDF抽出文を初期原稿として保存し、`pdf-extracted`、`ai`、`user`の由来を画面状態で管理する。抽出文・AI案はAIで置換可能にし、ユーザーが編集した非空原稿は保護する。
- WorkerはDynamoDBへ直接書き込まず、既存のS3 manifestに`progress`として工程、現在ページ、総ページ数、メッセージ、更新時刻を保存する。APIがポーリング時にその値をRenderRecordとプロジェクトの最新レンダリング概要へ反映する。
- プロジェクト一覧は最新レンダリング概要を表示し、進捗画面への復帰と、完成済みMP4の再ダウンロードを提供する。
- 元PDF名はS3キーではなくプロジェクトの`source.fileName`へ安全に保存する。ダウンロード時はJST開始日時を使い、例として`源内ハンズオン勉強会_20260822-084553.mp4`を`Content-Disposition`で返す。
- 短いページ別原稿には、有効な東京リージョン推論プロファイル`jp.anthropic.claude-haiku-4-5-20251001-v1:0`を使用する。

### この時点で実装済みの基盤

- shared typesに`Manifest.progress`、`RenderProgress`、`source.fileName`を追加
- フロントエンドAPI契約にプロジェクト種別、最新レンダリング概要、ダウンロード名、原稿由来を追加
- PDFを描画せずに本文だけを抽出する`extractPdfPageText()`を追加
- プロジェクトレコードへ`kind`と`latestRender`を保存できる型を追加
- PDFの表示名を安全化して`source.fileName`へ保存できるAPI契約を追加

### 未完了の実装・検証

- `VideoStudioPage`への抽出本文初期入力、AI案のページ内フィードバック、字幕設定の復元、ページ進捗表示
- `HomePage`での最新レンダリング表示、進捗画面復帰、MP4再ダウンロード
- 各Workerのmanifest進捗更新、APIの進捗同期、ダウンロード名用`ResponseContentDisposition`
- Bedrockモデルの切り替え
- 関連Vitest、TypeScript build、lint、CDK synth/diffの実行

### Git / PR判断

- 現在の作業ブランチは`fix/pipeline-and-frontend-design`。
- 作業ツリーには追跡済み38ファイルの変更と、PDF・MP4・確認用HTML・資料などの未追跡ファイルが混在している。
- この時点では、機能がエンドツーエンドで未完了かつ未検証のため、PRは作成しない。
- 実装・検証後に今回のVideo Studio改善へ関係するファイルだけを整理し、生成物や無関係な変更を除外したPR内容を先に確認してもらう。PRタイトル・本文の承認、作成の明示承認を受けるまで、PR作成・コミット・pushは行わない。
- 新規デプロイは明示承認まで実施しない。

---

## 2026-08-15 Bedrockモデル選択・概算コスト表示の調査ログ

### 相談内容

東京リージョンから利用できるClaude・Amazon Novaなどを、概算コストとともにユーザーが選べるようにするべきか、現行でモデルIDを固定している理由を調査した。

### 確認できた現行実装

- `infra/lib/slide-generator-construct.ts` がLambda環境変数 `BEDROCK_MODEL_ID` を `jp.anthropic.claude-haiku-4-5-20251001-v1:0` に固定している。
- `lambdas/slide-generator/src/bedrock-client.ts` はプロバイダー固有の `InvokeModel` リクエストではなく、Bedrock `Converse` APIを使用している。このため、Converse対応モデルであれば呼び出し形式をモデルごとに大きく分岐せずに扱える。
- UI、APIの入力契約、サーバー側バリデーションにはモデル選択項目がない。現状で利用者がモデルを変更するにはCDK設定を変更してデプロイする必要がある。
- 旧モデル `apac.anthropic.claude-sonnet-4-20250514-v1:0` はLegacy扱いによる実行拒否が発生したため、短いページ別ナレーションの既定値として、有効な日本向け推論プロファイルである現行Haiku 4.5へ切り替えている。

### 読み取り専用のAWS照会で確認した事項

- AWSアカウント `285336573977`、リージョン `ap-northeast-1` に対して照会した。
- 日本向け推論プロファイルとして、Claude Haiku 4.5、Claude Sonnet 4.5、Claude Sonnet 4.6、Amazon Nova 2 LiteのACTIVE状態を確認した。
- `jp.` 推論プロファイルは日本の地理的境界を維持するが、処理先は東京または大阪になり得る。東京単独が必須の要件とは区別して画面へ表示する必要がある。
- Nova Micro、Nova Lite、Nova Proなどの候補も東京リージョンで確認できた。ただし、`apac.` 推論プロファイルは日本以外のAPACリージョンへルーティングされ得るため、日本限定要件の既定候補にはしない。
- 東京オンデマンドのPrice Listでは、Nova Micro、Nova Lite、Nova 2 Liteについて入力・出力トークン単価を確認できた。一方、Claude 4.5系の現行単価は同じ照会経路から確定できなかったため、未確認の金額をUIへ固定表示してはいけない。

### 次期ステップの課題

1. **モデルカタログをサーバー側に導入する。** UIは生の`modelId`ではなく、`narration-recommended`のような`modelKey`だけを送信する。サーバー側で、許可済みのモデルIDまたは推論プロファイル、用途、地域範囲、出力トークン上限、状態、単価を解決する。
2. **推奨既定値を維持したプルダウンを実装する。** 初期値はClaude Haiku 4.5とし、ナレーション、スライド構成、マルチモーダル処理など用途別に候補を分ける。任意のBedrockモデルIDをクライアントから透過させない。
3. **概算と実績を分けて記録・表示する。** 実行前は入力推定トークンと最大出力トークンによる概算を表示する。実行後はBedrock応答の入力・出力トークン数、選択モデル、価格カタログの版をジョブへ保存し、実績ベースの概算を表示する。LLM推論費とPolly、MediaConvert、S3、Lambdaなどの動画生成費は別表示にする。
4. **料金カタログの信頼性を担保する。** AWS公式料金・契約料金を確認した単価だけをサーバー管理のカタログへ登録し、適用リージョン、推論プロファイル、オンデマンド・バッチ・キャッシュなどの条件と更新日を保持する。価格を確認できない候補は選択不可または「価格確認中」と表示する。
5. **権限・品質・費用上限をモデルごとに検証する。** Bedrock IAM権限を許可対象の推論プロファイルとモデルARNへ絞る。候補ごとにConverse呼び出し、構造化出力、既存プロンプト、出力パーサー、地域範囲を検証し、高額モデルにはロール制御とリクエスト・月額上限を設ける。
6. **実装前にデータ処理範囲の要件を決定する。** 「日本国内（東京・大阪可）」か「東京のみ」かを明確化し、それに合うモデルID・推論プロファイルだけをカタログへ採用する。

### 判断

- 現行の固定モデルは、Legacyモデルの実行拒否から復旧し、利用可能性・費用・出力形式を一組として安全に運用するための妥当な既定値である。
- 今後は固定値を完全に自由入力へ置き換えるのではなく、**推奨既定値 + サーバー管理の許可リスト式プルダウン + バージョン管理された料金カタログ**へ移行する。
- この調査ではコード、AWS設定、デプロイを変更していない。モデル選択機能の実装は上記課題を次期作業として扱う。
