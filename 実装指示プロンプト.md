# 実装指示プロンプト（slide-first-ai-video 差し替え用）

このファイルは、生成AIへそのまま渡すための指示書です。低コストのモデルでも迷わず実装できるように、**画面・データ契約・工程・受け入れ基準**を先に固定しています。

## 使い方

次の3つをまとめて渡してください。

| 渡すもの | 役割 |
| --- | --- |
| このファイル（実装指示プロンプト.md） | 何を作るか、守る規則、完了条件 |
| `PDFからMP4を作成する方法.md` | 動画生成の検証済み手順と失敗パターン |
| `mockup/` 一式（HTML・CSS・JS） | UIと画面遷移の正本 |

- 添付できない場合は、このファイルの内容だけでも成立するようにしています。
- 対象リポジトリ: `https://github.com/hide-G/slide-first-ai-video`（ブランチ `main`）

---

## ここから下を生成AIへの指示として渡す

### 0. あなたの役割

あなたは、既存リポジトリ `slide-first-ai-video` の実装を、下記の仕様に一致するよう作り直す実装担当です。

**最重要の前提**

1. `mockup/` のHTMLが**UIの正本**です。画面・項目・遷移はここに合わせます。モックに無い機能を追加しないでください。
2. `PDFからMP4を作成する方法.md` が**動画生成手順の正本**です。ここに書かれたコマンドと失敗パターンは、実測に基づく確定情報として扱ってください。
3. 本セクション「4. データ契約」が**サーバー側の正本**です。ここを最初にコードとして固定し、以降は契約に従って実装します。

推測で設計を変えないでください。仕様が足りない場合は、勝手に決めずに「不明点」として報告し、既定値を明示してから進めます。

### 1. 作るもの（1文で）

ログイン後、**生成AIでスライドを作る機能**と、**PDF/PowerPointをナレーション・字幕付き動画に変換する機能**を、それぞれ独立して使えるWebアプリを、AWS上にデプロイ可能な形で実装する。

### 2. 画面と機能（mockup/ に対応）

| 画面 | ファイル | 必須要件 |
| --- | --- | --- |
| ① ログイン | `mockup/index.html` | メール/パスワード、シングルサインオン、UI表示言語の切替（日本語/English） |
| ハブ | `mockup/home.html` | ②と③の入口、最近のプロジェクト一覧 |
| ② スライド作成 | `mockup/slide-studio.html` | 3ステップ: 入力 → 骨子レビュー → 生成・書き出し |
| ③ 動画作成 | `mockup/video-studio.html` | 4ステップ: 素材 → 出力設定 → ナレーションと読み方 → 生成 |

**②の必須要件**

- UI表示言語とは**別に**、スライドの言語（日本語 / English）を選べる。
- 文章と参考文献URL（複数）を入力できる。
- スライドを生成する**前に**、生成AIが作った骨子をユーザーが参照し、加筆・修正・並べ替え・追加・削除できる。ユーザーが確定した骨子だけをスライド生成へ渡す。
- 生成後、Markdown・PDF・PowerPointを書き出す。

**③の必須要件**

- ②を経由せず、単独でPDF/PowerPointをアップロードして動画化できる。これは省略不可の要件です。
- 出力サイズを選べる: `16:9` 1920×1080 / `9:16` 1080×1920 / `1:1` 1080×1080 / `4:5` 1080×1350。9:16と4:5では配置・余白色・セーフエリア表示を指定できる。
- ページごとのナレーション案を生成AIが自動作成し、ユーザーが編集できる。
- 英単語・固有名詞の読み方、振り仮名、間、速度をSSMLで指定できる。資料全体に効く読み方辞書も持つ。
- SSMLを選んだときに、Amazon PollyのSSMLチートシートを**移動できる小窓**として表示する。編集画面をふさがず、原稿を書きながら参照できること。各タグは選択中のエンジンでの対応状況を表示し、対応していないタグの挿入操作は無効にする。
- 字幕は「焼き込み」「別ファイル（SRT）」「なし」から選べる。

### 3. 技術スタックと配置（変更しない）

既存構成に合わせてください。新しい言語・パッケージマネージャ・フレームワークへ乗り換えないでください。

- Node.js 22 / TypeScript / pnpm ワークスペース（`packages/*`, `infra`, `lambdas/*`, `frontend`, `config`）
- インフラは AWS CDK（`infra/lib` の構成単位ごとのコンストラクト方式を維持）
- テストは vitest
- 動画処理は既存の `packages/renderer-port` の抽象を維持し、実装は `packages/renderer-ffmpeg` を使う
- FFmpegの実行基盤は **FFmpeg同梱のLambdaコンテナイメージ** を既定とする。理由: 検証済みのFFmpegコマンドをそのまま移植できる。1回の呼び出しを短く保つため、ページ単位に分割して並列実行する。

### 3.5 ローカル手順（PowerShell）をAWSへ読み替える

`PDFからMP4を作成する方法.md` は **Windows + PowerShell で手作業で検証した記録**です。**コマンド列をそのまま移植してはいけません。** 移植するのは次の2つだけです。

1. 使うコマンドと引数の意味（例: 出力時間を `-t` に実測値で渡す）
2. 検証すべき数値と判定条件（例: 全体長と音声合計の差が0.2秒以内）

#### 読み替え表

| ローカル（Windows / PowerShell） | AWS（Linux / Node.js） |
| --- | --- |
| 行継続のバッククォート | 使わない。引数は**配列**で渡す |
| `; if ($LASTEXITCODE -ne 0) { throw }` | `execFile` / `spawn` の終了コードを判定して失敗として扱う |
| シェル文字列を組み立てて実行 | シェルを経由しない（`shell: false`）。**原稿・辞書・ファイル名をコマンド文字列へ埋め込まない** |
| `E:\...`、`.\deck.pdf` | `/tmp` 配下の絶対パス。Lambdaで書き込めるのは `/tmp` だけ |
| ローカルにインストール済みの ffmpeg / ffprobe / marp | **コンテナイメージへ同梱**し、実行パスを環境変数で明示する |
| Windows同梱の日本語フォント | イメージへ日本語フォント（Noto Sans JP など）を導入し、フォントキャッシュを更新する |
| PowerShellのUTF-8 BOM問題 | 該当しない。代わりにロケール（`LANG` / `LC_ALL`）とUTF-8での入出力を確認する |
| 目視で画面を確認 | PNGを成果物として保存し、テストで検査できる形にする |
| 実行時間の制限なし | Lambdaは最大15分。**ページ単位に分割して並列実行**し、1回の実行を短く保つ |
| ローカルディスク | `/tmp` は既定512MB、最大10,240MB。使い終わったら削除する |

- 参照: [Lambdaの一時ストレージ設定](https://docs.aws.amazon.com/lambda/latest/dg/configuration-ephemeral-storage.html)

#### 実行イメージで必要なもの

| 工程 | 必要なもの | 注意 |
| --- | --- | --- |
| スライド生成（Marp） | Marp CLI と **Chromium系ブラウザ** | Marp CLIはPDF・PPTX・画像の変換にブラウザを使う。`CHROME_PATH` で実行ファイルを指定する。公式のDockerイメージ（`marpteam/marp-cli`）もある |
| PDF → PNG | PDFラスタライザ（poppler の `pdftoppm` など） | ローカル検証ではPython（PyMuPDF）を使ったが、本体はNode構成に合わせる。Pythonを使う場合はその工程だけ別イメージにする |
| PPTX → PDF | LibreOffice（ヘッドレス） | イメージが大きくなるため、**この工程だけ独立したイメージ**にする |
| 音声・動画 | FFmpeg と FFprobe | 同じイメージに入れる。バージョンをログに残す |

- 参照: [Marp CLI README](https://github.com/marp-team/marp-cli/blob/main/README.md)

#### 字幕の焼き込み

字幕テキストを `drawtext` のような**コマンド引数へ文字列として埋め込まないでください**。SRTまたはASSのファイルを書き出し、`subtitles` フィルタでファイルを参照して焼き込みます。理由: 記号や改行のエスケープ事故を防ぎ、原稿の内容がコマンドとして解釈される余地をなくすためです。

#### 検証の読み替え

`ffprobe` での仕様確認と `ffmpeg -v error -i <出力> -map 0 -f null -` でのデコード確認は、AWS上でも有効です。ただし手で実行するのではなく、**テストコードの中で実行して数値を判定**してください。許容差は第4.3節の不変条件に従います。

### 4. データ契約（最初にこれをコードへ固定する）

**最初のタスクとして**、この契約を `packages/shared-types` に型と実行時バリデーションとして実装し、`docs/contract.md` に文書化してください。以降のすべての工程はこの契約だけを介して連携します。

#### 4.1 S3レイアウト

```text
s3://<bucket>/users/{userId}/projects/{projectId}/
  input/source.pdf | input/source.pptx
  deck/deck.md, deck/deck.pdf, deck/deck.pptx
  pages/page-001.png, page-002.png, ...
  audio/page-001.mp3, page-002.mp3, ...
  captions/captions.srt
  clips/page-001.mp4, page-002.mp4, ...
  output/{renderId}/video.mp4
  manifest.json
```

#### 4.2 manifest.json（この資料の唯一の正本）

```json
{
  "schemaVersion": 1,
  "projectId": "p_0001",
  "userId": "u_0001",
  "contentLanguage": "ja",
  "source": { "kind": "generated", "fileKey": "deck/deck.pdf", "pageCount": 5 },
  "voice": { "id": "Takumi", "engine": "neural", "languageCode": "ja-JP", "sampleRate": "24000" },
  "output": {
    "aspect": "16:9",
    "width": 1920,
    "height": 1080,
    "fps": 30,
    "captions": "burn",
    "verticalLayout": null,
    "padColor": null
  },
  "lexicon": [{ "written": "Kiro Crew", "reading": "キロクルー", "method": "sub" }],
  "pages": [
    {
      "pageNumber": 1,
      "imageKey": "pages/page-001.png",
      "script": { "mode": "plain", "text": "この資料では、..." },
      "audioKey": "audio/page-001.mp3",
      "audioDurationSec": 27.912,
      "clipKey": "clips/page-001.mp4"
    }
  ],
  "stages": {
    "pages": "done",
    "audio": "done",
    "captions": "done",
    "clips": "running",
    "concat": "pending"
  }
}
```

- `source.kind`: `"generated"`（②で作成） | `"uploaded"`（③へ直接アップロード）
- `script.mode`: `"plain"` | `"ssml"`
- `lexicon[].method`: `"sub"` | `"phoneme"` | `"spell"`
- `stages` の各値: `"pending"` | `"running"` | `"done"` | `"failed"`

#### 4.3 破ってはいけない不変条件

1. `pages.length === source.pageCount`
2. 音声工程を開始する前に、すべての `script.text` が空でない
3. `audioDurationSec` は **ffprobeで実測したMP3の長さ**。推定値・概算値を書き込んではいけない
4. 各ページ動画の長さは、対応する `audioDurationSec` と一致する（許容差 0.05 秒）
5. 最終動画の長さは `audioDurationSec` の合計と一致する（許容差 0.2 秒）
6. 字幕のタイムコードは `audioDurationSec` の累積から生成する。別の推定値を使ってはいけない
7. 画面に表示する推定秒数は目安であり、`manifest.json` には書き込まない

### 5. API（画面の操作と1対1で対応させる）

すべてCognitoの認証必須。`projectId` は必ず認証ユーザーの所有物か検証する。

| 操作（画面） | API |
| --- | --- |
| プロジェクト作成 | `POST /projects` |
| 骨子の生成（②手順1） | `POST /projects/{id}/outline` |
| 骨子の確定保存（②手順2） | `PUT /projects/{id}/outline` |
| スライド生成（②手順3） | `POST /projects/{id}/deck` |
| 素材アップロード（③手順1） | `POST /projects/{id}/source-upload-url` → 署名付きURLへPUT → `POST /projects/{id}/source` |
| 出力設定の保存（③手順2） | `PUT /projects/{id}/output` |
| ナレーション案の生成（③手順3） | `POST /projects/{id}/narration` |
| ナレーションと辞書の確定保存 | `PUT /projects/{id}/narration` |
| 動画生成の開始（③手順4） | `POST /projects/{id}/renders` → `renderId` を返す |
| 進行状況の取得 | `GET /projects/{id}/renders/{renderId}` |
| 成果物の取得 | `GET /projects/{id}/renders/{renderId}/artifacts`（署名付きURL） |

### 6. 工程（それぞれ単独で再実行できるようにする）

| 工程 | 入力 | 出力 | 完了条件 |
| --- | --- | --- | --- |
| 1 pages | `input/source.*` または `deck/deck.pdf` | `pages/page-NNN.png` | PNG枚数 = ページ数 |
| 2 audio | 確定した `script` + `voice` | `audio/page-NNN.mp3` と実測 `audioDurationSec` | 全ページに実測値が入る |
| 3 captions | `script` + `audioDurationSec` | `captions/captions.srt` | 字幕数 = ページ数、時刻が単調増加 |
| 4 clips | PNG + MP3 + 実測秒数 | `clips/page-NNN.mp4` | 各clip長 = 音声長（±0.05秒） |
| 5 concat | clips（+ 字幕焼き込み） | `output/{renderId}/video.mp4` | 全体長 = 音声合計（±0.2秒） |

- 途中で失敗したら、その工程だけを再実行できること。全部やり直しにしない。
- 原稿が変わっていない場合、音声を作り直さずに別サイズの動画を書き出せること（`script.text` のハッシュで判定）。

### 7. 実装で必ず守る規則（実測で確認済みの内容）

1. **ページ動画の長さは `-t` に実測秒数を明示指定する。** `-loop 1` と `-shortest` の組み合わせだけに頼ると、音声より約1.4〜1.7秒長いページ動画が発生した。
2. **PowerPointは本物のPPTXとして出力する。** PDFのバイト列を `.pptx` という名前で返す実装は禁止。ZIP内部に `ppt/presentation.xml` とページ数分のスライドXMLがあることをテストで確認する。
3. **SSMLは音声とエンジンの対応状況に従う。** 検証環境（`Takumi` / `neural` / `ja-JP`）では `<speak>…</speak>` は成功したが、`<prosody rate="95%">` を加えると `InvalidSsmlException` になった。Amazon Pollyの対応表では、ニューラル音声で `<emphasis>` は使用不可、`<prosody>` と `<say-as>` は一部のみ対応、`<sub>` `<phoneme>` `<break>` は対応となっている。UIでは選択中の音声・エンジンで使えないタグを無効化し、失敗時はユーザーへ理由を返す。原稿はXMLエスケープしてからSSMLへ入れる。
   - 参照: [Supported SSML tags](https://docs.aws.amazon.com/polly/latest/dg/supportedtags.html)
4. **音声は24kHzを想定し、AACのビットレートは96kbps程度にする。** 192kbpsを指定するとFFmpegが上限に合わせて下げる警告を出した。
5. **スライドの見た目は画像で確認する。** 生成成功だけでは、白背景に白文字のような不具合を検出できない。表紙・表・注意書き・出典ページを含む代表ページのPNGを比較テストの対象にする。
6. **字幕と音声は同じ確定原稿から作る。** 別々に生成してずれる状態を作らない。

### 7.5 費用表示（工程ごとのコスト）

各工程でかかった費用を画面に表示します。**推定額と実績額を必ず区別**してください。混ぜて1つの数字にしてはいけません。

#### 表示する2種類の値

| 種類 | 出し方 | 表示タイミング | 画面での扱い |
| --- | --- | --- | --- |
| 推定額 | 実測した使用量 × 単価 | 工程の完了直後 | 「推定」と明記する |
| 実績額 | コスト配分タグ + Cost Explorer / CUR | あとから照合 | 未確定のうちは「集計待ち」と表示する |

コスト配分タグは有効化から**反映まで最大24時間**かかり、**遡及されません**（過去分はバックフィル要求が必要）。したがって生成直後に実績額を出すことはできません。この制約を理由に推定額の表示を省略しないでください。

- 参照: [Application inference profiles のコスト管理](https://docs.aws.amazon.com/bedrock/latest/userguide/cost-mgmt-application-inference-profiles.html)

#### 使用量の取得元（推測しない）

| 対象 | 取得元 |
| --- | --- |
| Amazon Bedrock | `Converse` レスポンスの `usage.inputTokens` / `usage.outputTokens`（キャッシュ利用時は `cacheReadInputTokens` / `cacheWriteInputTokens` も記録） |
| Amazon Polly | `SynthesizeSpeech` レスポンスヘッダ `x-amzn-RequestCharacters`。**SSMLタグは課金対象文字数に含まれない** |
| AWS Lambda | 各実行の課金対象時間と割り当てメモリから GB秒 を求める |
| Amazon S3 | 工程が書き込んだオブジェクト数と合計バイト数 |
| AWS Step Functions | 状態遷移回数 |

- 参照: [SynthesizeSpeech](https://docs.aws.amazon.com/polly/latest/APIReference/API_SynthesizeSpeech.html) / [Amazon Polly の制限](https://docs.aws.amazon.com/polly/latest/dg/limits.html) / [Converse API](https://docs.aws.amazon.com/bedrock/latest/userguide/conversation-inference.html)

#### 単価の扱い

- 単価をソースコードへ直接書かないでください。**AWS Price List API（`GetProducts`）から取得**し、キャッシュして使います。
- 計算に使った単価と**取得日を manifest に記録**します。あとから同じ数字を再現できるようにするためです。
- 通貨はUSDを基準にし、無料利用枠は考慮しないことを画面に明記します。

#### manifest への追記

```json
{
  "cost": {
    "currency": "USD",
    "priceListFetchedAt": "2026-08-15T00:00:00Z",
    "stages": [
      {
        "stage": "audio",
        "service": "polly",
        "usage": { "billedCharacters": 921 },
        "estimatedCost": 0.0147
      },
      {
        "stage": "clips",
        "service": "lambda",
        "usage": { "gbSeconds": 96.5 },
        "estimatedCost": 0.0016
      }
    ],
    "estimatedTotal": 0.0412,
    "actual": { "status": "pending", "amount": null, "reconciledAt": null }
  }
}
```

#### タグ付け（実績照合のため）

- 費用が発生するリソースと呼び出しに、`projectId`、`renderId`、`userId` をタグとして付けます。
- Bedrockは**アプリケーション推論プロファイル**を作成してタグを付け、呼び出し時に指定します。これによりモデル呼び出し単位の実績を追えます。
- 付けたタグは請求コンソールでコスト配分タグとして有効化する必要があります。この手順を `docs/cost.md` に記載してください。

#### 画面表示（mockup に対応）

- ③の生成完了画面: 工程・サービス・使用量・推定コストの表と、推定合計、実績は集計待ちである旨。
- ③のナレーション編集画面: 現在の原稿の課金対象文字数と、音声合成の推定コスト。
- ②の生成完了画面: 骨子生成・スライド生成・保存の推定コスト。
- ハブ画面のプロジェクト一覧: プロジェクトごとの推定コスト列。

#### 費用表示に関する禁止事項

- 推定額を「実際の請求額」として表示すること
- 使用量を推測して費用を出すこと（必ずAPIの戻り値や実測値を使う）
- 単価をコードに固定して、取得日を記録しないこと
- 費用計算のために追加のモデル呼び出しや音声合成を行うこと

### 8. 禁止事項

- 失敗を隠すフォールバック（中身の違うファイルを成功として返す、空ファイルを返す、例外を握りつぶす）
- `audioDurationSec` に推定値を入れる
- PowerShell前提のコマンド列をそのまま移植すること（第3.5節の読み替え表に従う）
- 原稿・辞書・ファイル名をシェルのコマンド文字列へ埋め込むこと
- モックアップに無い機能・画面・設定項目の追加
- 生成した動画・音声・PDFをGitへコミットすること
- 認証なしで到達できるAPIやバケットの公開設定
- 資格情報・トークン・個人情報をログや例外メッセージへ出力すること
- 検証していないのに「完了」と報告すること

### 9. 既存コードの扱い

1. まず現状を棚卸しし、`docs/migration.md` に「既存の何を、新しいどの工程へ対応させるか」を表で書く。
2. 新仕様に対応するものは再利用する。対応しないものは、デプロイされる経路から外す。削除する場合は理由を書く。
3. 判断がつかないものは削除せず、`docs/migration.md` に「保留」として記録し、報告する。
4. 既存テストは消さない。仕様が変わった場合は、変更理由をコミットメッセージに書いて更新する。

### 10. 進め方（1タスクずつ、必ず検証してから次へ）

各タスクの完了時に、**実行したコマンドと出力の要点**を報告してください。検証していない項目は「未検証」と明記してください。

| # | タスク | 検証方法 |
| --- | --- | --- |
| 1 | データ契約を型・実行時バリデーション・`docs/contract.md` として実装 | 不正なmanifestを弾く単体テストが通る |
| 2 | 既存コードの棚卸しと `docs/migration.md` 作成 | 表に既存の全コンストラクト・全Lambdaが載っている |
| 3 | 実行イメージの用意（FFmpeg・FFprobe・Marp・ブラウザ・PDFラスタライザ・日本語フォント） | 各コマンドのバージョンがログに出る。日本語を含むPNGが正しく描画される（文字が□にならない） |
| 3.1 | 工程1（PDF/PPTX → PNG） | 5ページのPDFからPNGが5枚。枚数不一致で失敗すること |
| 4 | 工程2（Polly音声 + 実測秒数） | 各MP3が再生可能。`audioDurationSec` がffprobeの値と一致 |
| 5 | 工程3（SRT生成） | 字幕数 = ページ数。時刻が単調増加。累積が音声合計と一致 |
| 6 | 工程4（ページ動画） | 各clip長と音声長の差が0.05秒以内 |
| 7 | 工程5（連結・字幕焼き込み） | 全体長と音声合計の差が0.2秒以内。`ffmpeg -v error -i out.mp4 -map 0 -f null -` が終了コード0 |
| 8 | 工程の連結と再実行（失敗した工程だけ再開） | 工程4だけを失敗させ、工程4以降のみが再実行されること |
| 9 | API実装（第5節の表のとおり） | 認証なしで401。他人の`projectId`で403 |
| 10 | ②スライド作成の画面と接続 | 骨子を編集して生成すると、編集後の文章がPDFに入る |
| 11 | ③動画作成の画面と接続（単独アップロード経路を含む） | ②を経由せずPDFをアップロードして動画が完成する |
| 12 | 出力サイズ（9:16など）と字幕3種 | 9:16で1080×1920になる。SRT選択時にSRTが出る |
| 13 | 費用の計測と表示（第7.5節） | 使用量がAPIの戻り値と一致。推定額と実績待ちが区別されて表示される |
| 14 | CDKでデプロイ可能な状態にする | `cdk synth` が成功。バケットが非公開。API全経路に認証 |
| 15 | 全体確認 | 第11節の受け入れ基準をすべて満たす |

小さな差分で進めてください。1タスクで複数の関心事を同時に変更しないでください。

### 11. 受け入れ基準（これを満たしたら完成）

1. ②で骨子を修正してからスライドを生成でき、修正内容がPDF・PPTX・Markdownに反映される。
2. ③に外部で作ったPDFを直接アップロードして、動画を生成できる。
3. 5ページの資料で、最終動画の長さと音声合計の差が0.2秒以内。ページごとの差が0.05秒以内。
4. `9:16` を選ぶと1080×1920の動画が出力される。
5. 字幕を「焼き込み」にすると映像に入り、「別ファイル」にするとSRTが出力される。
6. SSMLで指定した読み方が音声へ反映される。使えないタグは、失敗理由をユーザーへ返す。
7. SSMLチートシートが移動できる小窓として開き、選択中のエンジンでの対応状況が表示される。原稿の編集を妨げない。
8. `ffmpeg -v error -i <出力> -map 0 -f null -` が終了コード0で終わる。
9. PPTXがPowerPointで開ける本物のファイルである（ZIP内部構造をテストで確認）。
10. UI表示言語（日本語/English）とスライドの言語が独立して動作する。
11. モックアップにあるボタン・入力項目が、すべて対応する処理へつながっている。押しても何も起きない要素が無い。
12. 認証なしでAPIへ到達できない。S3バケットが公開されていない。
13. 工程ごとの推定コストが表示され、使用量がAPIの戻り値と一致する。実績額は「集計待ち」と区別して表示される。
14. 生成されたスライド画像と焼き込み字幕で、日本語が正しく表示される（文字が□にならない）。
15. 外部コマンドの呼び出しがシェルを経由せず、原稿や辞書の内容がコマンド文字列へ埋め込まれていない。
16. `pnpm install && pnpm build && pnpm test` と `cdk synth` が成功する。

### 12. 費用を抑えるための決まり

- 生成AIと音声合成は、ユーザーが明示的に押した操作でだけ呼ぶ。画面表示や自動保存で呼ばない。
- 原稿のハッシュが変わっていなければ、音声を作り直さない。
- 別サイズの書き出しは、既存の音声とページ画像を再利用する。
- 開発中の動作確認は、5ページ程度の小さな資料で行う。

### 13. 報告の形式

各タスクの終わりに、次の形式で短く報告してください。

```text
タスク: <番号と名前>
変更したファイル: <一覧>
実行した検証: <コマンドと結果の要点>
数値の実測値: <該当する場合。例: 音声合計 139.488秒 / 動画 139.558秒 / 差 0.070秒>
未検証の項目: <あれば明記。無ければ「なし」>
不明点・確認したいこと: <あれば1〜3件>
```

### 14. 迷ったときの判断基準

1. モックアップに答えがあるか → あればそれに従う。
2. `PDFからMP4を作成する方法.md` に答えがあるか → あればそれに従う。
3. データ契約（第4節）に答えがあるか → あればそれに従う。
4. どれにも無い → 既定値を決めて実装し、「決めた内容と理由」を報告する。仕様を勝手に広げない。
