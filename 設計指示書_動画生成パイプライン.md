# 設計指示書: 動画生成パイプラインの改修

この文書は、**未知を事前に実機検証で潰したうえで作った実装指示**です。ここに書かれた数値・設定・バージョンは、すべて実際のAWS環境で動作確認した実測値です。

## 0. この文書の使い方

- 対象リポジトリ: `https://github.com/hide-G/slide-first-ai-video`（`main` から作業ブランチを切る）
- 併せて読む文書: `実装指示プロンプト.md`（アプリ全体の仕様）、`docs/contract.md`（データ契約）、`mockup/`（UIの正本）
- **この文書に書かれた値を推測で変えないこと。** 検証済みの事実です。
- 判断に迷ったら、勝手に決めずに「不明点」として報告してください。

---

## 1. 絶対制約

1. **Dockerは使用できません。** 開発機がMacBook上のBoot Campで動くWindows 10のため、Docker自体を導入できません。コンテナイメージ方式を提案・実装しないでください。
2. **次の外部プログラムは使用禁止です。** Lambdaの`nodejs22.x`ランタイムに存在しないためです。
   - `ffmpeg`、`ffprobe`
   - `pdftoppm`（poppler）
   - `libreoffice`
3. すべてのLambdaはZip形式の`nodejs22.x`とします。
4. 外部コマンドを呼ぶ場合はシェルを経由せず、引数は配列で渡します。原稿やファイル名をコマンド文字列へ埋め込まないでください。

既存コードには上記の禁止プログラムを呼ぶ実装が残っています。**それらは削除して置き換えます。** 各Lambdaディレクトリにある`Dockerfile`も削除してください。CDKから参照されておらず、実装方針と矛盾するためです。

---

## 2. 検証済みの事実

### 2.1 AWS Elemental MediaConvert（動画生成）

環境: `ap-northeast-1`、Defaultキュー（ON_DEMAND、ACTIVE）。専用IAMロールは未作成なのでCDKで作成します。

**次のジョブ設定で実際にCOMPLETEしました。** これをそのまま雛形として使ってください。

```json
{
  "Role": "<MediaConvert用IAMロールのARN>",
  "AccelerationSettings": { "Mode": "DISABLED" },
  "Settings": {
    "TimecodeConfig": { "Source": "ZEROBASED" },
    "Inputs": [
      {
        "TimecodeSource": "ZEROBASED",
        "AudioSelectors": {
          "Audio Selector 1": {
            "DefaultSelection": "DEFAULT",
            "ExternalAudioFileInput": "s3://<bucket>/users/<userId>/projects/<projectId>/audio/page-001.wav"
          }
        },
        "VideoGenerator": {
          "Duration": 25200,
          "ImageInput": "s3://<bucket>/users/<userId>/projects/<projectId>/pages/page-001.png",
          "FramerateNumerator": 30,
          "FramerateDenominator": 1,
          "Width": 1920,
          "Height": 1080
        }
      }
    ],
    "OutputGroups": [
      {
        "Name": "File Group",
        "OutputGroupSettings": {
          "Type": "FILE_GROUP_SETTINGS",
          "FileGroupSettings": { "Destination": "s3://<bucket>/users/<userId>/projects/<projectId>/output/<renderId>/" }
        },
        "Outputs": [
          {
            "NameModifier": "-video",
            "ContainerSettings": { "Container": "MP4", "Mp4Settings": {} },
            "VideoDescription": {
              "Width": 1920,
              "Height": 1080,
              "CodecSettings": {
                "Codec": "H_264",
                "H264Settings": {
                  "RateControlMode": "QVBR",
                  "MaxBitrate": 5000000,
                  "FramerateControl": "SPECIFIED",
                  "FramerateNumerator": 30,
                  "FramerateDenominator": 1
                }
              }
            },
            "AudioDescriptions": [
              {
                "AudioSourceName": "Audio Selector 1",
                "CodecSettings": {
                  "Codec": "AAC",
                  "AacSettings": { "Bitrate": 96000, "CodingMode": "CODING_MODE_2_0", "SampleRate": 48000 }
                }
              }
            ]
          }
        ]
      }
    ]
  }
}
```

`Inputs` はページ数ぶん並べます。MediaConvertは**追加した順に連結**し、1ジョブで最大150入力です。

**実測結果**

| 項目 | 値 |
| --- | --- |
| ページ数 | 2 |
| 指定した表示時間 | 25,176ms と 27,840ms（合計53,016ms） |
| `GetJob` が返した `DurationInMs` | 53,066ms |
| 差 | +50ms |
| 出力解像度 | 1920×1080 |
| 映像トラック長 | 53.067秒 |
| 音声トラック長 | 53.099秒 |
| ファイルサイズ | 4,220,313バイト（53秒） |

**差が出た原因は判明しています。** MediaConvertは入力ごとに表示時間をフレーム単位へ切り上げます。30fpsでは1フレーム33.3333msです。

- 25,176ms → 756フレーム → 25,200ms（+24ms）
- 27,840ms → 836フレーム → 27,866.67ms（+26.67ms）

**対処: こちら側で先にフレーム境界へ丸めて渡します。**（第4.2節）

出力の長さは `GetJob` のレスポンスから取得できます。ffprobeは不要です。

```text
Job.OutputGroupDetails[0].OutputDetails[0].DurationInMs
```

### 2.2 Chromium + Marp（スライド生成とPDFラスタライズ）

**Lambda上で次の6項目が動作することを確認しました。**

検証したLambda設定:

| 項目 | 値 |
| --- | --- |
| ランタイム | `nodejs22.x` |
| アーキテクチャ | `x86_64` |
| メモリ | 3008MB |
| タイムアウト | 300秒 |
| 一時ストレージ | 2048MB |
| Zipサイズ | 93.4MB |
| 展開後サイズ | 約165MB（上限250MB） |
| 実行時間 | 4,168ms（コールドスタート含む、全6項目を1回の実行で完了） |

検証したパッケージ:

| パッケージ | バージョン |
| --- | --- |
| `@sparticuz/chromium` | 133.0.0 |
| `puppeteer-core` | 24.2.0 |
| `@marp-team/marp-core` | 4.1.0 |
| `pdfjs-dist` | 4.10.38 |
| `@fontsource/noto-sans-jp` | 5.2.5 |

**検証結果**

| 確認項目 | 結果 |
| --- | --- |
| Chromiumの展開と起動 | 成功。`chromium.executablePath()` が `/tmp/chromium` を返し、`HeadlessChrome/133.0.6943.0` が起動 |
| Marp CoreでのHTML生成 | 成功。HTML 9,986文字、CSS 49,600文字 |
| 日本語フォントの適用 | 成功。`document.fonts.check("48px ProbeJP")` が `true`。同じ文字列の描画幅が、埋め込みフォント 528px、未適用時 316.85px で異なる |
| PDF生成 | 成功。103,859バイト、2ページ、`%PDF-` で始まる |
| ページ単位のPNG | 成功。`section` 要素ごとに2枚（50,163バイト、37,315バイト） |
| ブラウザ内 pdf.js でのラスタライズ | 成功。2ページ認識、canvas 1440×810 でPNG化、サンプリングした色が6種類（白紙でない） |

**Chromiumの起動方法（この形で成功しました）**

```js
const chromium = require("@sparticuz/chromium");
const puppeteer = require("puppeteer-core");

const executablePath = await chromium.executablePath();
const browser = await puppeteer.launch({
  args: chromium.args,
  executablePath,
  headless: chromium.headless,
  defaultViewport: { width: 1920, height: 1080 },
});
```

`chromium.args` をそのまま渡します。サンドボックス無効化などの必要なフラグが含まれています。自分でフラグを足さないでください。

**日本語フォントの与え方（この形で成功しました）**

LambdaのChromiumにはシステムフォントがありません。フォントファイルをbase64にしてCSSへ埋め込みます。fontconfigの設定は不要です。

```js
const base64 = fs.readFileSync(fontPath).toString("base64");
const fontCss = `
@font-face {
  font-family: 'NotoSansJP';
  src: url(data:font/woff2;base64,${base64}) format('woff2');
  font-weight: 400;
  font-display: block;
}
section, section * { font-family: 'NotoSansJP', sans-serif !important; }
`;
```

使用するフォントファイルは1つだけです。

```text
node_modules/@fontsource/noto-sans-jp/files/noto-sans-jp-japanese-400-normal.woff2   （1.1MB）
```

`setContent` の後に `await page.evaluate(() => document.fonts.ready)` を必ず入れてください。

**日本語が適用されたかを検証する方法**

「生成できた」だけでは豆腐（□）に気付けません。次の比較で判定してください。

```js
const applied = await page.evaluate(() => {
  const sample = "日本語の漢字とひらがな";
  const ctx = document.createElement("canvas").getContext("2d");
  ctx.font = "48px NotoSansJP";
  const a = ctx.measureText(sample).width;
  ctx.font = "48px NoSuchFontFamilyXYZ";
  const b = ctx.measureText(sample).width;
  return Math.abs(a - b) > 0.5;
});
```

`false` の場合はフォントが当たっていません。処理を失敗させてください。

**PDFのサイズ換算に注意**

`page.pdf({ width: "1920px", height: "1080px" })` で作られるPDFは **1440×810ポイント** になります（96dpi→72ptの換算）。このPDFを1920×1080のPNGへラスタライズするには倍率が必要です。

```text
scale = 1920 / 1440 = 1.3333
```

検証では `scale: 1` で 1440×810 のcanvasになりました。実装では `getViewport({ scale: 1920 / viewportAt1.width })` のように、目的の幅から倍率を求めてください。

**pdf.js をブラウザ内で使う方法（この形で成功しました）**

Node側でpdf.jsを使うのではなく、Chromiumのページ内で動かします。ネイティブモジュールもWASMパッケージの選定も不要になります。

```js
await page.evaluate(async (libSource, workerSource, dataBase64) => {
  const libUrl = URL.createObjectURL(new Blob([libSource], { type: "text/javascript" }));
  const workerUrl = URL.createObjectURL(new Blob([workerSource], { type: "text/javascript" }));
  const pdfjs = await import(libUrl);
  pdfjs.GlobalWorkerOptions.workerSrc = workerUrl;

  const bytes = Uint8Array.from(atob(dataBase64), (c) => c.charCodeAt(0));
  const doc = await pdfjs.getDocument({ data: bytes }).promise;
  // ページごとに canvas へ描画し、canvas.toDataURL("image/png") で取り出す
}, libSource, workerSource, pdfBase64);
```

使うファイルは次の2つです。パッケージ全体は同梱しません。

```text
node_modules/pdfjs-dist/build/pdf.min.mjs          （344KB）
node_modules/pdfjs-dist/build/pdf.worker.min.mjs   （1,344KB）
```

---

## 3. パッケージング規則

現在の不具合の直接原因です。**必ず守ってください。**

### 3.1 やってはいけないこと

`tsc` の出力だけを `lambda.Code.fromAsset("../lambdas/<name>/dist")` に渡すこと。

現状の`marp-render`、`caption-worker`、`clip-worker`、`concat-worker`がこれに該当します。資産には`index.js`しか入らず、`require("@slide-first/shared-types")` が解決できずに `Runtime.ImportModuleError` で起動しません。実測で資産は4〜8ファイル・8〜18KBしかありませんでした。

### 3.2 正しい方法

| Lambda | 方法 |
| --- | --- |
| `api`、`polly-worker`、`slide-generator` | 現状の `NodejsFunction`（esbuildで自動バンドル）を維持。変更不要 |
| `caption-worker` | esbuildで単一ファイルにバンドル。外部依存なし |
| `mediaconvert-worker`（新規） | esbuildで単一ファイルにバンドル。AWS SDKはランタイム同梱なので `--external:@aws-sdk/*` |
| `marp-render` | esbuildでバンドルしつつ、`@sparticuz/chromium` と `puppeteer-core` は `--external` にして `node_modules` を同梱。アセット3ファイルも同梱 |

`marp-render` の資産構成は次のとおりです。

```text
lambdas/marp-render/dist/
├─ index.js                      # esbuildの出力
├─ assets/
│  ├─ noto-sans-jp.woff2         # 1.1MB
│  ├─ pdf.min.mjs                # 344KB
│  └─ pdf.worker.min.mjs         # 1,344KB
└─ node_modules/                 # @sparticuz/chromium と puppeteer-core とその依存のみ
```

容量を250MB以内に収めるため、`node_modules` から次を除外してください。検証時はこれで展開後165MBに収まりました。

- `@napi-rs`（36MB。ブラウザ内でラスタライズするため不要）
- `@fontsource`（82MB。上記woff2 1ファイルだけ`assets/`へコピーして本体は除外）
- `pdfjs-dist`（35MB。上記2ファイルだけ`assets/`へコピーして本体は除外）
- すべての `*.map`

### 3.3 検証方法

ビルド後、次を必ず確認してください。

1. `cdk synth` 後に `infra/cdk.out/asset.<hash>/` の中身を見る
2. `caption-worker` と `mediaconvert-worker` は `index.js` 1ファイルであること
3. `marp-render` は `index.js`、`assets/`（3ファイル）、`node_modules/` があり、展開後200MB未満であること
4. どの資産にも `@slide-first/` を`require`したまま解決できない状態が残っていないこと

---

## 4. データ契約の変更

`packages/shared-types` と `docs/contract.md` を更新します。

### 4.1 音声はWAVにする

Amazon Pollyの出力を`mp3`から`pcm`へ変更し、WAVヘッダを付けて保存します。理由は、ffprobeを使わずに**長さを厳密に計算できる**ためです。

```text
audioDurationSec = PCMのバイト数 / (2 × sampleRate)
```

PollyのPCMは16bit符号付き・1チャンネルです。WAVヘッダは44バイトなので、長さの計算にはヘッダを除いたデータ部のバイト数を使ってください。

**`sampleRate` は `16000` を使います。** PCM出力で指定できるのは `8000` と `16000` だけです。`24000` は`mp3`用の値で、`pcm`で渡すと `Invalid SampleRate parameter` で失敗します（実機で発生させて確認済み）。無効な値が保存されていた場合は `16000` へ寄せてください。

- 参照: [SynthesizeSpeech の SampleRate](https://docs.aws.amazon.com/polly/latest/dg/API_SynthesizeSpeech.html)

- `pages[].audioKey` の拡張子を `.wav` に変更します
- MediaConvertはWAVを入力として受け付けます

### 4.2 フレーム境界へ丸めた表示時間を持たせる

`pages[]` に次を追加します。

```json
{
  "pageNumber": 1,
  "audioDurationSec": 25.176,
  "frameAlignedDurationMs": 25200
}
```

計算方法は次のとおりです。

```text
frameMs   = 1000 × framerateDenominator / framerateNumerator   （30fpsなら33.3333）
frames    = ceil(audioDurationSec × 1000 / frameMs)
durationMs = round(frames × frameMs)
```

- MediaConvertの各入力の `VideoGenerator.Duration` には `frameAlignedDurationMs` を渡します
- 字幕のタイムコードも `frameAlignedDurationMs` の累積で計算します。`audioDurationSec` の累積を使わないでください。両者が混ざると音声と字幕がずれます

### 4.3 許容差の改訂

現在の `docs/contract.md` は「全体0.2秒以内」となっていますが、これはFFmpegでの実測に基づく値です。MediaConvertではページ数が増えると超えるため、次に変更します。

| 検証項目 | 新しい判定 |
| --- | --- |
| ページごと | `VideoGenerator.Duration` が `frameAlignedDurationMs` と完全一致（計算で保証されるため差0） |
| 全体 | `GetJob` の `DurationInMs` と `frameAlignedDurationMs` の合計の差が **50ms以内** |
| 音声との関係 | 各ページの `frameAlignedDurationMs` が `audioDurationSec × 1000` 以上、かつ超過が1フレーム（34ms）以内 |

実測では合計53,066msに対し丸め後の合計53,066.67msで、差はほぼ0でした。

### 4.4 費用情報にMediaConvertを追加

`cost.stages[]` に `mediaconvert` を追加します。

```json
{ "stage": "video", "service": "mediaconvert", "usage": { "outputDurationSec": 139.6, "outputResolution": "1920x1080" }, "estimatedCost": 0.0 }
```

単価は引き続きAWS Price List APIから取得し、取得日を記録します。コードへ直接書かないでください。

---

## 5. 工程の新しい構成

| 工程 | 実装 | 入力 | 出力 | 使う技術 |
| --- | --- | --- | --- | --- |
| 0 スライド生成 | `marp-render`（`action: "generateDeck"`） | 確定した骨子 | `deck/deck.md`、`deck.pdf`、`deck.pptx`、`pages/page-NNN.png` | Marp Core + Chromium |
| 1 pages | `marp-render`（`stage: "pages"`） | ②由来なら工程0のPNGを使う。③のPDFアップロードならPDF | `pages/page-NNN.png` | ブラウザ内 pdf.js |
| 2 audio | `polly-worker` | 確定した原稿 | `audio/page-NNN.wav` と厳密な長さ | Polly（PCM出力） |
| 3 captions | `caption-worker` | 原稿 + `frameAlignedDurationMs` | `captions/captions.srt` | 純粋なJavaScript |
| 4 video | `mediaconvert-worker`（新規） | PNG + WAV + 表示時間 | `output/<renderId>/video.mp4` | MediaConvert |

`clip-worker` と `concat-worker` は削除します。工程4と5が1つのMediaConvertジョブに統合されます。

ステートマシンは `pages → audio → captions → video` の4段になります。完了待ちには CDK の `MediaConvertCreateJob` タスクを `IntegrationPattern.RUN_JOB` で使ってください。ポーリング用のLambdaを自作しないでください。

**IAMの注意点**: MediaConvertには専用のサービスロールが必要で、ジョブを投入する側には `iam:PassRole` の許可が必要です。ここは失敗しやすい箇所です。付与する`PassRole`の対象は、作成したMediaConvertロールのARNだけに限定してください。

---

## 6. PPTXの扱い

- **③のPPTXアップロードは受け付けません。** `POST /projects/{id}/source` で拡張子が`.pptx`の場合、HTTP 400 とエラーコード `PPTX_NOT_SUPPORTED` を返し、「PowerPointでPDFに書き出してからアップロードしてください」という趣旨のメッセージを日英で返してください。変換にLibreOfficeが必要で、この構成では動かせないためです。
- **②のPPTX書き出しは提供します。** ただしMarpのPPTXは各ページを画像として貼り付けた形式で、テキストは編集できません。画面に「画像として書き出されるため、テキスト編集はできません」という注記を表示してください（`mockup/slide-studio.html` の書き出し欄に対応する注記を追加済み）。

---

## 7. タスク一覧

**1タスクずつ進め、各タスクの検証を通してから次へ進んでください。** 各タスクの完了時に第9節の形式で報告してください。

| # | タスク | 検証方法と期待値 |
| --- | --- | --- |
| 1 | `caption-worker` をesbuildバンドルに変更 | 資産が `index.js` 1ファイルになる。`node_modules` が無い |
| 2 | フレーム丸め関数を `packages/core` に実装 | 25,176ms→25,200ms、27,840ms→27,867ms を返す単体テストが通る |
| 3 | PCM長さ算出とWAVヘッダ生成を `packages/core` に実装 | 16,000Hz・16bit・モノラルで、データ部806,400バイト→25.2秒を返す単体テストが通る |
| 4 | `polly-worker` をPCM出力＋WAV保存に変更 | 出力が`.wav`。`audioDurationSec` が計算値と一致 |
| 5 | 契約の更新（`frameAlignedDurationMs` 追加、`audioKey`を`.wav`、許容差改訂） | 不正なmanifestを弾く単体テストが通る。`docs/contract.md` が更新されている |
| 6 | `caption-worker` を `frameAlignedDurationMs` 基準に変更 | 字幕数=ページ数。時刻が単調増加。累積が丸め後合計と一致 |
| 7 | `mediaconvert-worker` を新規実装 | 第2.1節のJSONと同じ構造のジョブ設定を生成する単体テストが通る |
| 8 | `clip-worker`、`concat-worker`、各`Dockerfile`、FFmpeg関連コードを削除 | `pnpm build` と `pnpm test` が通る。`ffmpeg` の文字列が実装コードから消える |
| 9 | CDK変更（ワーカー入替、MediaConvertロール、`PassRole`、ステートマシン4段化） | `cdk synth` 成功。`PassRole` の対象がMediaConvertロールのARNのみ |
| 10 | `marp-render` に `action: "generateDeck"` を実装 | Marp CoreでHTML生成→PDF・PNG・PPTX出力。日本語適用チェック（第2.2節）が`true` |
| 11 | `marp-render` の工程1をブラウザ内pdf.jsに変更 | 5ページのPDFからPNGが5枚。幅1920pxになっている |
| 12 | `marp-render` のパッケージングとアセット同梱 | 展開後200MB未満。`cdk synth` 成功 |
| 13 | PPTXアップロードの400エラーと画面注記 | `.pptx` で400と `PPTX_NOT_SUPPORTED` が返る |
| 14 | 費用表示にMediaConvertを追加 | 画面に行が出る。単価の取得日が記録される |
| 15 | 全体確認 | 第8節の受け入れ基準を満たす |

---

## 8. 受け入れ基準

1. `pnpm install && pnpm build && pnpm test` が成功する
2. `cdk synth` が成功する
3. すべてのLambda資産に、解決できない`require`が残っていない
4. `ffmpeg`、`ffprobe`、`pdftoppm`、`libreoffice` を呼ぶコードが存在しない
5. `Dockerfile` が存在しない
6. 5ページの資料で、`GetJob` の `DurationInMs` と `frameAlignedDurationMs` の合計の差が50ms以内
7. 各ページの `frameAlignedDurationMs` が音声長以上で、超過が34ms以内
8. 生成したスライドPNGで日本語が正しく描画される（第2.2節の幅比較が`true`）
9. ③にPDFをアップロードして動画が完成する
10. ③に`.pptx`をアップロードすると400と`PPTX_NOT_SUPPORTED`が返る
11. ②の書き出し欄に、PPTXのテキスト編集不可の注記がある
12. 工程ごとの推定コストにMediaConvertが含まれる
13. 認証なしでAPIへ到達できない。S3バケットが公開されていない

---

## 9. 報告フォーマット

各タスクの終わりに、次の形式で短く報告してください。

```text
タスク: <番号と名前>
変更したファイル: <一覧>
実行した検証: <コマンドと結果の要点>
数値の実測値: <該当する場合。例: 丸め後合計 53,067ms / DurationInMs 53,066ms / 差 1ms>
未検証の項目: <あれば明記。無ければ「なし」>
不明点: <あれば1〜3件>
```

---

## 10. 禁止事項

- Dockerやコンテナイメージを使う方式を提案・実装すること
- `ffmpeg`、`ffprobe`、`pdftoppm`、`libreoffice` を使うこと
- `tsc` の出力だけを `Code.fromAsset` に渡すこと
- 第2節の検証済みの値・設定・バージョンを推測で変更すること
- `audioDurationSec` に推定値を入れること（PCMのバイト数から計算する）
- 字幕のタイムコードを `frameAlignedDurationMs` 以外から計算すること
- 失敗を隠すフォールバック（中身の違うファイルを返す、例外を握りつぶす、空ファイルを返す）
- 工程の失敗を戻り値だけで表し、ステートマシン側で判定しないこと（実行が`SUCCEEDED`のまま成果物が無い状態になります。第11.2節(3)）
- 読み込まれないことを確認せずにパッケージを同梱対象から除外すること（第11.2節(1)）
- 読む側だけを実装して、`manifest.json` を書き出す側を用意しないこと（第11.2節(4)）
- モックアップに無い機能・画面・設定項目を追加すること
- 生成した動画・音声・PDFをGitへコミットすること
- 資格情報やトークンをログや例外メッセージへ出力すること
- 検証していないのに「完了」と報告すること

---

## 11. 実機検証の結果（2026-08-15）

第2節はパイプラインを作る前の要素検証です。この節は**実装後に通しで動かした結果**です。

環境: スタック `ltvideo-dev`、`ap-northeast-1`。経路③（PDFアップロード）で3ページの日本語PDFを投入。

### 11.1 通しで動作することを確認しました

| 確認項目 | 実測値 |
| --- | --- |
| Step Functions | `SUCCEEDED`。4工程すべて `success=true` |
| `manifest.json` | S3に生成（2,849バイト） |
| ページPNG | 3枚（ページ数と一致）。各1920×1080 |
| 音声WAV | 3本 |
| 字幕SRT | 3件（ページ数と一致）。タイムコードが累積 `frameAlignedDurationMs` と一致 |
| 動画MP4 | 1,028,081バイト。MediaConvertジョブ `COMPLETE`、入力3件を連結 |
| `frameAlignedDurationMs` 合計 | 24,800ms |
| `GetJob` の `DurationInMs` | 24,833ms |
| 差 | **33ms**（判定基準の50ms以内） |
| ページごとの実測 | p1 7.7175秒→7,733ms / p2 7.5425秒→7,567ms / p3 9.4925秒→9,500ms |
| 日本語描画 | 正常。豆腐（□）なし |

出力ファイル名は `page-001-video.mp4` になります。MediaConvertは**最初の入力のファイル名**に `NameModifier` を付けて出力名を決めるためです。連結された1本の動画であり、1ページ目だけの動画ではありません。長さで判別できます。

### 11.2 実装が動かなかった原因（すべて修正済み）

デプロイは成功し、Step Functionsも `SUCCEEDED` を返すのに、**成果物が1つも生成されない**状態でした。原因は次の5件です。同じ失敗を繰り返さないために記録します。

**(1) 除外したパッケージが実行時に必要だった**

`marp-render` の同梱対象から `@puppeteer/browsers` を「バイナリのダウンロード専用だから不要」として外していました。実際には `puppeteer-core` の `ChromeLauncher.js` が読み込み時に `import` します。

```text
Cannot find package '@puppeteer/browsers' imported from
/var/task/node_modules/puppeteer-core/lib/esm/puppeteer/node/ChromeLauncher.js
```

第3.2節のとおり `node_modules` を手で同梱する方式では、**推移的な依存を自分で解決する責任が生じます。** 除外は「サイズが大きいから」ではなく「読み込まれないことを確認したから」を根拠にしてください。

対策として、ビルド時に**同梱した各パッケージの `dependencies` が揃っているかを検査**し、欠けていればビルドを失敗させます。この検査で今回の不具合は検出できます。除外を増やすときはこの検査を通してください。

同梱結果: 77パッケージ、98.4MB（除外解除前は42パッケージ、93.9MB）。

**(2) PollyのPCMで無効なサンプルレートを指定していた**

`24000` を指定して `Invalid SampleRate parameter` で失敗しました。第4.1節を修正済みです。PCMは `8000` と `16000` だけです。

**(3) 失敗が成功として報告されていた**

各工程は失敗しても例外を投げず `{ success: false, error }` を返します。Lambdaの呼び出し自体は成功するため、ステートマシンは次の工程へ進み、最後に `SUCCEEDED` で終わっていました。**「完了したのに動画が無い」状態の直接原因です。**

各工程の後に `Choice` を置き、`$.<stage>Result.Payload.success` が `false` なら `Fail` 状態へ遷移させます。

```text
pages → 判定 → audio → 判定 → captions → 判定 → video → 判定 → Succeed
          ↓失敗       ↓失敗        ↓失敗       ↓失敗
                          Fail（Error: StageFailed）
```

実機で確認済みです。工程1〜3を飛ばしてMediaConvertを失敗させると、実行が `FAILED` になりました。

**(4) `manifest.json` を書き出す実装が存在しなかった**

パイプラインの各工程は `manifest.json` だけを正本として読みます。一方でAPIは状態をDynamoDBに保存しており、**両者をつなぐ実装がどこにもありませんでした。** 画面から実行しても工程1が manifest を見つけられません。

`POST /projects/{id}/renders` で、DynamoDBのレコードから manifest を組み立ててS3へ書き出します。書き出す前に `ManifestSchema.parse` を通し、契約に合わない状態でパイプラインへ渡さないようにします。あわせて次を400で弾きます。

- 素材が未登録、またはページ数が不正: `SOURCE_REQUIRED`
- ナレーションがページ数分そろっていない、または空: `NARRATION_REQUIRED`

**契約の正本を1つに保つ設計にした場合、その正本を誰がいつ書くのかも実装対象です。** 読む側だけを作ると通しで動きません。

**(5) 任意項目を省略すると500になった**

`PUT /projects/{id}/narration` で `voice` を省略すると次のエラーになりました。

```text
Invalid UpdateExpression: An expression attribute value used in expression is not defined; attribute value: :voice
```

DynamoDBの更新式を組み立てる処理が、値が `undefined` の項目も `#key = :key` として式に含めていました。値の側は落ちるため、参照だけが残ります。**任意項目は更新式から外してください。**

### 11.3 残作業

次の3件は未実装です。**画面で選べるのに動作に反映されません。** 実装するときは仕様の判断が必要なので、勝手に決めずに確認してください。

**(1) 出力の縦横比・解像度が反映されない**

`manifest.output` の `aspect` / `width` / `height` を読んでいるコードがありません。`9:16`（1080×1920）を指定しても、ページPNGも動画も1920×1080で出力されます。

固定値の箇所:

| ファイル | 箇所 |
| --- | --- |
| `lambdas/marp-render/src/index.ts` | `page.pdf({ width: "1920px", height: "1080px" })`、`scale = 1920 / viewportAt1.width`、`defaultViewport: { width: 1920, height: 1080 }` |
| `lambdas/mediaconvert-worker/src/job-builder.ts` | `VideoGenerator` の `Width: 1920 as const` / `Height: 1080 as const`、`VideoDescription` の `Width: 1920` / `Height: 1080` |
| `lambdas/mediaconvert-worker/src/index.ts` | 費用記録の `outputResolution: "1920x1080"` |

**数値を差し替えるだけでは終わりません。** アップロードされたPDFは16:9です。9:16の枠に入れるにはレイアウトの決め方が必要で、`manifest` の `verticalLayout` と `padColor` がそのための項目ですが、どちらも未使用です。次のどれにするかを決めてから着手してください。

- スライドを上部に置いて下を余白にする（`verticalLayout` で切り替える）
- 中央に置いて上下を帯にする（`padColor` を使う）
- MediaConvert側の拡縮に任せる（余白は黒固定になります）

**(2) `fps` の扱いが工程間で食い違っている**

`lambdas/polly-worker/src/index.ts` は `manifest.output.fps` を読んで `frameAlignedDurationMs` を算出しますが、`job-builder.ts` は `FramerateNumerator: 30` 固定です。`fps: 60` を選ぶと、字幕の想定尺と動画の実尺がページごとにずれて累積します。30fpsでは露見しません。

`job-builder.ts` が `manifest.output.fps` を読むようにしてください。第4.2節の丸め計算と同じ値を使う必要があります。

**(3) `captions: "burn"` が未実装**

MediaConvertのジョブに字幕の設定（`CaptionSelectors` / `CaptionDescriptions`）がありません。サイドカーのSRTだけが出ます。画面では「焼き込み」を選べます。

実装するにはフォント・文字サイズ・位置・背景の指定が必要です。Lambdaと違いMediaConvert側のフォント指定になるため、日本語表示の確認も別途必要です。

### 11.4 検証手順の記録

同じ検証を再実行するときの手順です。

1. Cognitoに検証用ユーザーを作成し、`admin-set-user-password --permanent` でパスワードを確定してから `initiate-auth --auth-flow USER_PASSWORD_AUTH` でIDトークンを取得します（アプリクライアントはシークレット無し）
2. `POST /projects` → `POST /source-upload-url` → 署名付きURLへPDFをPUT → `POST /source` → `PUT /output` → `PUT /narration` → `POST /renders` の順に呼びます
3. `describe-execution` で状態を追い、`s3api list-objects-v2` で成果物を数えます
4. `mediaconvert list-jobs` で該当ジョブを探し、`OutputGroupDetails[0].OutputDetails[0]` の `DurationInMs` と `VideoDetails` を確認します

IDトークンの有効期限は1時間です。長い検証では再取得してください。

Windows環境の注意点です。

- PowerShell 5.1 の `Get-Content -Raw` はCP932で読むため、UTF-8のJSONに日本語が含まれると壊れます。**JSONの解釈はNode側で行ってください。**
- PowerShell 5.1 では `[System.Security.Cryptography.RandomNumberGenerator]::GetBytes()` が使えません。`RNGCryptoServiceProvider` を使います。
